<div class="text-center">
<h1>チャットルーム</h1>
  
</div>

<!--<h4>参加者</h4>-->
<!--<% @entries.each do |e| %>-->
<!--  <h5><strong><a href="/users/<%= e.user.id %>"><%= e.user.name %>さん</a></strong></h5>-->
<!--<% end %>-->

<hr>
<% if @messages.present? %>
  <% @messages.each do |m| %>
    <% if m.user_id == current_user.id %>
      <div  class="text-right">
      <strong><%= m.content %></strong>
      <small>:<strong><%= m.user.name %></strong></small>
      <hr>
      </div>
    <% else %>
      <div class="text-left">
      <strong><%= m.content %></strong>
      <small>by <strong><%= m.user.name %></strong></small>
      <hr>
      </div>
    <% end %>
  <% end %>
<% else %>
  <h3 class="text-center">メッセージはまだありません</h3>
<% end %>

<%= form_for @message do |f| %>
  <%= f.text_field :content, :placeholder => "メッセージを入力して下さい" , class: 'form-control', rows: 3, :size => 70 %>
  <%= f.hidden_field :room_id, :value => @room.id %>
  <br>
  <%= f.submit "投稿する", class: 'btn btn-primary' %>
<% end %>